# Production frontend environment variables
# VUE_APP_API_URL is set to /api so requests proxy through nginx to the backend
VUE_APP_API_URL=/api

# Stripe publishable key (safe to expose in frontend)
# Replace with your actual Stripe publishable key before deploying
VUE_APP_STRIPE_PUBLISHABLE_KEY=pk_live_replace_with_your_key
